serviceAccount:
  name: minestom-lobby

groundsToken:
  enabled: true

permissions:
  enabled: true
  serviceUrl: http://service-permissions-runtime.default.svc.cluster.local:8080
  access:
    - capability: snapshot:read
