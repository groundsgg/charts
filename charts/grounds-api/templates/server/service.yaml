{{- if .Values.server.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-server-http-service
  labels:
    {{- include "common.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
    {{- with .Values.global.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.global.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: server
{{- end }}

