apiVersion: v1
kind: Service
metadata:
  name: {{ include "grounds-valkey-dev.fullname" . }}
spec:
  selector: { app: {{ include "grounds-valkey-dev.fullname" . }} }
  ports: [{ name: valkey, port: 6379, targetPort: 6379 }]
